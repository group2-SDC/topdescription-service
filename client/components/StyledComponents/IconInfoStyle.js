import styled from 'styled-components';

const IconInfoStyle = styled.section`
    display: flexbox;
    justify-content: flex-start;
    padding-top: 6px;
    font-size: 9px;
    color: black;
    padding-left: 3px;
    padding-right: 3px;

    &:hover {
        cursor: pointer;
        text-decoration: underline;
    }
`;

export default IconInfoStyle;